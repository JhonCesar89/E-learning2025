import React from 'react';

// Ensure the file is saved with a .tsx extension for JSX support

export const Button = ({ children }: { children: React.ReactNode }) => {
    return <button>{children}</button>;
  };
  
  // Add other components if needed like Modal, Navbar, etc.
  